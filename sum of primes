import math
n=int(input("enter range:"))
primes=[]
for a in range(2,n+1):
    x=int(math.sqrt(a))
    for i in range(2,x+1):
        if (a%i==0):
            break
        else:
            primes.append(a)
print(sum(primes))
